import "./App.css";

function App() {
  return (
    <>
      <button
        onClick={() => {
          // setSelectedTrackId(null);
          // setSelectedTrack(null);
        }}>
        Resset selection
      </button>
    </>
  );
}

export default App;
